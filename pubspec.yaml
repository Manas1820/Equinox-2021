name: equinox_21
description: A new Flutter application.

version: 1.0.2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  timeline_tile:
  google_fonts:
  provider:
  http:
  cloud_firestore:
  firebase_auth:
  firebase_core:
  flutter_svg:
  url_launcher:
  flutter_statusbarcolor: ^0.2.3
  firebase_messaging: ^7.0.3
  connectivity_wrapper: ^1.0.4
  flutter_feather_icons: ^1.0.3
  circular_countdown_timer: ^0.1.0
  carousel_slider: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/
    - images/Equinox Logo-02 4.png
    - images/google-logo.png
